# VoxCPM TTS Tool — runtime + dev dependencies
# This is the single source of truth for what gets installed by run.sh / run.ps1.
# pyproject.toml mirrors the production deps for `pip install -e .` to work,
# but the launcher always installs from this file.

# ---- Runtime ------------------------------------------------------------

# PyTorch is installed first by run.ps1/run.sh from the CUDA wheel index.
# Keeping these here documents the runtime requirement and lets pip/uv skip
# them when already satisfied.
torch>=2.5.0
torchaudio>=2.5.0

# voxcpm SDK is installed separately by run.ps1/run.sh with --no-deps.
# Reason: voxcpm 2.0.2 declares torchcodec, but torchcodec has no Windows
# wheels; the app avoids torchaudio's torchcodec-dependent load path.
transformers>=4.36.2
einops
inflect
addict
wetext
datasets>=3,<4
pydantic
tqdm
simplejson
sortedcontainers
librosa
matplotlib
argbind
safetensors

# voxcpm 2.0.2 transitively requires gradio>=6,<7 — keep aligned.
gradio>=6,<7
numpy
soundfile
huggingface_hub
modelscope
funasr

# ---- Dev ----------------------------------------------------------------

pytest
pytest-mock
